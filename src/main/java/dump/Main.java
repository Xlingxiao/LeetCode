package dump;

import pojo.ListNode;
import utils.ListNodeUtils;

public class Main {
    public static void main(String[] args) {
        while (true) {
            ListNodeUtils.getListNode(Integer.MIN_VALUE, Integer.MAX_VALUE, 500);
        }
    }
}
